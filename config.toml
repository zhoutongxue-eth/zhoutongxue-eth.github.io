baseURL = "https://zhoutongxue.xyz"
title   = "区块链周同学主页"
defaultContentLanguage = "zh-cn"
languageCode = "zh-cn"
theme = "hello-friend-ng"

PygmentsCodeFences = true
PygmentsStyle = "monokai"

paginate  = 10
rssLimit  = 10  # Maximum number of items in the RSS feed.
copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License." # This message is only used by the RSS template.

# googleAnalytics = ""
# disqusShortname = ""

archetypeDir = "archetypes"
contentDir   = "content"
dataDir      = "data"
layoutDir    = "layouts"
publishDir   = "public"

buildDrafts  = false
buildFuture  = false
buildExpired = false
canonifyURLs = true

enableRobotsTXT = true
enableGitInfo   = false
enableEmoji     = true
enableMissingTranslationPlaceholders = false
disableRSS     = false
disableSitemap = false
disable404     = false
disableHugoGeneratorInject = false

[permalinks]
  posts = "/posts/:year/:month/:title/"

[author]
  name = "区块链周同学"

[blackfriday]
  hrefTargetBlank = true

[taxonomies]
  tag      = "tags"
  category = "categories"

[params]
  dateform        = "Jan 2, 2006"
  dateformShort   = "Jan 2"
  dateformNum     = "2006-01-02"
  dateformNumTime = "2006-01-02 15:04"

  # Metadata mostly used in document's head
  # 
  description = "这里深度探讨区块链技术和Web3项目，目标是让每个人都看懂区块链。欢迎加入Discord社区，共同学习进步，一起探索未来"
  keywords = "blockchain,btc,bitcoin,web3,zhoutongxue"
  images = ["android-chrome-512x512.png"]

  # Home subtitle of the index page.
  #
  homeSubtitle = "欢迎点击下方按钮加入Discord社区，我们一起深度学习和讨论区块链、Web3的创新和项目。共同学习进步，一起探索未来"

  # Set a background for the homepage
  # backgroundImage = "assets/images/background.jpg"

  # Prefix of link to the git commit detail page. GitInfo must be enabled.
  #
  # gitUrl = ""

  # Set disableReadOtherPosts to true in order to hide the links to other posts.
  #
  disableReadOtherPosts = false

  # Enable theme toggle
  # 
  # This options enables the theme toggle for the theme. 
  # Per default, this option is off. 
  # The theme is respecting the prefers-color-scheme of the operating systeme. 
  # With this option on, the page user is able to set the scheme he wants. 
  enableThemeToggle = true

  # Sharing buttons
  #
  # There are a lot of buttons preconfigured. If you want to change them,
  # generate the buttons here: https://sharingbuttons.io
  # and add them into your own `layouts/partials/sharing-buttons.html`
  #
  enableSharingButtons = true

  # Global language menu
  #
  # Enables the global language menu.
  #
  enableGlobalLanguageMenu = false

  # Integrate Javascript files or stylesheets by adding the url to the external assets or by
  # linking local files with their path relative to the static folder, e.g. "css/styles.css"
  #
  customCSS = []
  customJS  = []

  # Toggle this option need to rebuild SCSS, requires extended version of Hugo
  #
  justifyContent = false  # Set "text-align: justify" to .post-content.

  # Custom footer 
  # If you want, you can easily override the default footer with your own content. 
  #
  [params.footer]
    trademark = true
    rss = false
    copyright = false
    author = true

    topText = []
    bottomText = [
      "Do Your Own Research",
      "自己做好研究"
    ]

  # Colors for favicons
  #
  [params.favicon.color]
    mask = "#1b1c1d"
    msapplication = "#1b1c1d"
    theme = "#1b1c1d"

  [params.logo]
    logoMark     = ">"
    logoText     = "cd /web3/"
    logoHomeLink = "/"
    # Set true to remove the logo cursor entirely.
    # logoCursorDisabled = false
    # Set to a valid CSS color to change the cursor in the logo.
    # logoCursorColor    = "#67a2c9"
    # Set to a valid CSS time value to change the animation duration, "0s" to disable.
    # logoCursorAnimate  = "2s"
    # Append the current url pathname to logoText 
    # logoCursorPathname = true

  # Commento is more than just a comments widget you can embed — 
  # it’s a return to the roots of the internet. 
  # An internet without the tracking and invasions of privacy. 
  # An internet that is simple and lightweight. 
  # An internet that is focused on interesting discussions, not ads. 
  # A better internet.
  # Uncomment this to enable Commento.
  #
  # [params.commento]
  #   url = ""

  # Uncomment this if you want a portrait on your start page
  #
  [params.portrait]
    path     = "/android-chrome-512x512.png"
    alt      = "Portrait"
    maxWidth = "100px"

  # Social icons
  [[params.social]]
    name = "discord"
    url  = "https://discord.gg/Zpxrh9gFQf"

  [[params.social]]
    name = "youtube"
    url  = "https://www.youtube.com/channel/UC_n3oCLsxw-ZeanT797oy-g"

  [[params.social]]
    name = "twitter"
    url  = "https://twitter.com/zhoutongxue_eth"

  [[params.social]]
    name = "github"
    url  = "https://github.com/zhoutongxue-eth"

#  [[params.social]]
#    name = "medium"
#    url  = "https://github.com/zhoutongxue-eth"

#[languages]
#  [languages.cn]
#    subtitle  = "欢迎"
#    weight    = 1
#    copyright = '<a href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank" rel="noopener">CC BY-NC 4.0</a>'

[menu]
  [[menu.main]]
    identifier = "posts"
	weight     = 1
    name       = "文章"
    url        = "posts/"
  [[menu.main]]
    identifier = "categories"
	weight     = 2
    name       = "分类"
    url        = "categories/"
  [[menu.main]]
    identifier = "tags"
	weight     = 3
    name       = "标签"
    url        = "tags/"
  [[menu.main]]
    identifier = "about"
	weight     = 4
    name       = "关于我"
    url        = "about/"
